# gh-publisher

[English](./README.md) · [简体中文](./README.zh-CN.md) · [हिन्दी](./README.hi.md) · [Español](./README.es.md) · [Français](./README.fr.md) · [العربية](./README.ar.md) · [বাংলা](./README.bn.md) · [Português](./README.pt.md) · [Русский](./README.ru.md) · [日本語](./README.ja.md)

[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](./LICENSE)
[![Version](https://img.shields.io/badge/version-1.3.1-blue.svg)](./CHANGELOG.md)
[![100% AI-crafted](https://img.shields.io/badge/100%25-AI--crafted-9cf.svg)](#disclaimer)

**git ছাড়াই GitHub-এ ফাইল প্রকাশ করুন — টোকেন-দক্ষ, গোপনীয়তা-নিরাপদ, মাল্টি-এজেন্ট, বহুভাষিক।**

`gh-publisher` হলো একটি এজেন্ট স্কিল, যা `gh` CLI + GitHub REST API (Contents / Git Database) ব্যবহার করে একটি স্থানীয় ডিরেক্টরিকে GitHub রিপোজিটরিতে প্রকাশ করে — git প্রয়োজন নেই। একটি পুনঃব্যবহারযোগ্য স্ক্রিপ্ট দিয়ে এটি খালি রিপো ইনিশিয়ালাইজ ও ব্যাচ কমিট সামলায়, ফলে এজেন্টরা পুরো API ফ্লো নতুন করে বের করার বদলে একটিমাত্র কমান্ডে পুশ করতে পারে। এটি **GitHub-এর সর্বাধিক ব্যবহৃত ১০টি ভাষা** (README অনুবাদ) প্রকাশ করে, আর স্থানীয় ওয়ার্কিং কপি আপনার পছন্দের ভাষায় থাকে।

## কেন এটি আলাদা

- **🚀 Git-মুক্ত পুশ** — git নেই এমন মেশিনেও কাজ করে; খালি রিপোজিটরি স্বয়ংক্রিয়ভাবে ইনিশিয়ালাইজ করে।
- **⚡ টোকেন-দক্ষ** — একটি `scripts/push.ps1` কমান্ডই স্ক্যান → ইনিশিয়ালাইজ → ব্যাচ কমিট → মাস্ক করা আউটপুট (একটি `PUSHED N files -> URL` লাইন) করে, কয়েক ডজন হাতে-লেখা API কলের বদলে।
- **🔒 গোপনীয়তা ও অ্যাকাউন্ট নিরাপত্তা** — টোকেন কেবল `gh` keyring-এ থাকে (কখনোই চ্যাট/লগ/ফাইলে নয়); পুশের আগে ফাইলগুলো সিক্রেট-স্ক্যান করা হয় (`github_pat_`, `ghp_`, `sk-`, প্রাইভেট কী…); আউটপুট মাস্ক করা হয়।
- **🔌 মাল্টি-এজেন্ট-অভিযোজ্য** — `pwsh` স্ক্রিপ্ট Windows/macOS/Linux-এ চলে; DSH / Codex / Claude Code ম্যাপিং নথিভুক্ত করা আছে; কোনো হার্ডকোড করা প্ল্যাটফর্ম টুলের নাম নেই।
- **🧩 স্বয়ংক্রিয় খালি-রিপো ইনিশিয়ালাইজ** — খালি রিপো শনাক্ত করে এবং ব্যাচ কমিটের আগে Contents API দিয়ে প্রথম ফাইল সিড করে।
- **🌍 বহুভাষিক প্রকাশ** — স্থানীয় ওয়ার্কিং কপি আপনার নির্বাচিত ভাষায় থাকে (ডিফল্ট চীনা); রিলিজে GitHub-এর সর্বাধিক ব্যবহৃত ১০টি ভাষার (en, zh-CN, hi, es, fr, ar, bn, pt, ru, ja) README অনুবাদ থাকে। দেখুন `references/i18n.md`।

## এটি কীভাবে কাজ করে

1. `pwsh -ExecutionPolicy Bypass -File scripts/push.ps1 -Source <dir> -Repo owner/repo -Message "msg" [-GhPath <path-to-gh.exe>] [-Languages en,zh-CN,hi,es,fr,ar,bn,pt,ru,ja]`
2. স্বয়ংক্রিয়ভাবে `gh` বাইনারি খুঁজে বের করে (`-GhPath` → PATH → সাধারণ ইনস্টল পাথ) এবং `GH_CONFIG_DIR` স্বয়ংক্রিয়ভাবে শনাক্ত করে — কোনো ম্যানুয়াল PATH/config সামঞ্জস্যের প্রয়োজন নেই।
3. সিক্রেট স্ক্যান → যেকোনো কী/টোকেন প্যাটার্ন পেলে বাতিল করে (যদি না `-ForceSecret` দেওয়া হয়)।
4. ব্রাঞ্চ ref চেক করে খালি রিপো শনাক্ত করে (404 = খালি) → প্রয়োজনে প্রথম ফাইল সিড করে (Contents API)।
5. সব ফাইল ব্যাচ কমিট করে (Git Database API: blobs → tree → commit → ref)।
6. `PUSHED N files -> https://github.com/owner/repo` প্রিন্ট করে — আর কিছুই নয়। রিপো না থাকলে → একটি `gh repo create` ইঙ্গিত প্রিন্ট করে।
7. ঐচ্ছিক `-Languages` চেক: পুশের আগে প্রতিটি ভাষার ফাইল বিদ্যমান কিনা যাচাই করে (যেমন `README.hi.md`) এবং কোনোটি অনুপস্থিত থাকলে সতর্ক করে।

## বহুভাষিক প্রকাশ (স্থানীয় ভাষা + ১০টি রিলিজ ভাষা)

- **স্থানীয় ওয়ার্কিং কপি**: আপনার কনফিগার করা ভাষায় থাকে (zh বা en) — ইনস্টল করা স্কিল ডিরেক্টরির `config.local.json`-এ (ডিফল্ট `zh`)। পরিবর্তন করতে বলুন *"স্থানীয় ডিফল্ট ভাষা ইংরেজিতে পরিবর্তন করুন"*; স্থানীয় SKILL.md-ও সেই অনুযায়ী আপডেট হয়।
- **রিলিজ**: `SKILL.md` ইংরেজিতে থাকে (সার্বজনীন GitHub প্রাথমিক সংস্করণ), আর সর্বাধিক ব্যবহৃত ১০টি ভাষার জন্য `README.<lang>.md` প্রকাশিত হয়। ভাষার তালিকা, অনুবাদের নিয়ম এবং ট্রিগার-কন্ট্রাক্ট নোটের জন্য দেখুন `references/i18n.md` (সব ভাষার সংস্করণে একই `name` থাকে)।

## এনভায়রনমেন্ট সেলফ-চেক (প্রথম পুশের আগে একবার)

- `Get-Command gh` (অথবা স্ক্রিপ্টকে ইনস্টল পাথ স্বয়ংক্রিয়ভাবে শনাক্ত করতে দিন); না থাকলে: `winget install GitHub.cli`।
- `gh auth status` একটি লগইন করা অ্যাকাউন্ট দেখায় (টোকেন `github_pat_***…` হিসেবে মাস্ক করা থাকে)।
- `gh api repos/{owner}/{repo}` অথবা `gh repo list` ডেটা ফেরত দেয়।

## ইনস্টল

```
~/.dsh/skills/gh-publisher/    # global
.dsh/skills/gh-publisher/      # per project
```

এটি ট্রিগার করতে *"এটি GitHub-এ পুশ করুন"*, *"এই স্কিলটি একটি রিপোতে প্রকাশ করুন"* — এর মতো বাক্য ব্যবহার করুন; অথবা **set-skill**-এর `/skill` মেনুর ⑤ নম্বর আইটেম দিয়ে।

## নথিপত্র

- `references/security.md` — গোপনীয়তা ও অ্যাকাউন্ট নিরাপত্তা
- `references/platform-adapter.md` — DSH / Codex / Claude Code ম্যাপিং
- `references/i18n.md` — বহুভাষিক প্রকাশ প্রোটোকল (১০টি ভাষা)

## সহযোগী স্কিল

- **[set-skill](https://github.com/tydm2/create-generate-skill)** — এই স্কিলটিকে `/skill` মেনুর ⑤ নম্বর আইটেম হিসেবে তালিকাভুক্ত করে।
- **[workflow-builder](https://github.com/tydm2/workflow-builder-skill)** — এটি যে মাল্টি-এজেন্ট ওয়ার্কফ্লো তৈরি করে, সেগুলো এই স্কিল দিয়ে প্রকাশ করুন।

## প্রয়োজনীয়তা

- `gh` CLI (`gh auth login` দিয়ে লগইন করা) + `pwsh` (PowerShell Core, ক্রস-প্ল্যাটফর্ম)।

## দাবিত্যাগ

> **এই স্কিলটি 100% AI-নির্মিত।** সমস্যা অনিবার্য — আলোচনা ও পুল রিকোয়েস্ট স্বাগত। লেখক বাস্তব ব্যবহারের ভিত্তিতে এটিতে সক্রিয়ভাবে ইটারেট করেন।

## লাইসেন্স

[MIT](./LICENSE)
